import {css} from '@linaria/core';

export default function Help() {
  return (
    <div
      className={css`
        display: flex;
        flex-direction: column;
        align-items: center;
      `}
    >
      <h1>Help</h1>
    </div>
  );
}
